/*
* Archivo de configuración de Capacitor para la aplicación de portafolio.
* Configura el nombre de la aplicación, el ID y el directorio web.
*/


import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mi-portafolio',
  webDir: 'www'
};

export default config;
